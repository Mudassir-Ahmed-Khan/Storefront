name: Check Python Version

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  check-python-version:
    runs-on: ubuntu-latest

    steps:
      - name: Check Python version
        run: python --version
